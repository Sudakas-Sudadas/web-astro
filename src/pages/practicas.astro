---
import Layout from '../layouts/Layout.astro';
import TextBlock from '../components/TextBlock.astro';
import Triangle from '../components/Triangle.astro';

import { getLinks } from '../utils/storyblok.js';

let links = await getLinks('practicas/');
links = Object.values(links);
---

<script>
    import { activateStoryblokBridge } from '../utils/storyblok';
    activateStoryblokBridge();
</script>

<Layout title='Compartimos Prácticas'>
    <figure class='container mx-auto mb-8'>
        <div class='mx-auto md:mr-12 sm:w-3/4 md:w-3/5 lg:w-1/2'>
            <TextBlock>
                <blockquote class='font-serif-ital'>
                    <p>
                        "Tu cuerpo territorio, es mi cuerpo territorio y cuando
                        nosotras decimos acuerparnos con amor, quiere decir que
                        tu lucha la re-significo en mi lucha. Acuerparnos es
                        decir que me pongo en tu cuerpo, y tú te pones en mi
                        cuerpo, y liberamos ese cuerpo juntas. Acuerpamiento, es
                        decir que entre los cuerpos entretejidos nos damos
                        fuerza para la liberación de este cuerpo que quizás está
                        desequilibrado, pero que puede trascender. O sea, lo
                        podemos liberar y sacarlo del patriarcado."
                    </p>
                </blockquote>
            </TextBlock>

            <figcaption class='px-4 py-1 text-right text-xs 2xl:text-sm'>
                —Lolita Chávez, <br />
                <cite
                    >Cuerpo Territorio, en <a
                        href='https://www.radiosavia.com/'
                        class='font-bold text-rosa-300'>Radio Savia</a
                    >
                </cite>
            </figcaption>
        </div>
    </figure>

    <div class='grow relative overflow-hidden py-8 md:py-12'>
        <Triangle
            class='absolute inset-0 w-1/2 md:w-1/3 lg:w-1/4 xl:w-1/6 mt-4 sm:mt-0 ml-auto -z-10 text-rosa-50'
        />

        <ul class='container mx-auto px-4 lg:px-12 font-bold space-y-4'>
            {
                links.map((link: any) => (
                    <li>
                        <a
                            href={`/${link.slug}`}
                            class='block p-4 border border-rosa-100 text-rosa-300'
                        >
                            {link.name}
                        </a>
                    </li>
                ))
            }
        </ul>
    </div>
</Layout>
